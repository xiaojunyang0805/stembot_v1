/**
 * Authentication Components Barrel Export
 *
 * Centralized export for all authentication-related components
 * to simplify imports throughout the application.
 *
 * Location: src/components/auth/index.ts
 */

export { LoginForm } from './LoginForm'
export { GoogleAuthButton } from './GoogleAuthButton'

// Export default as well for backward compatibility
export { default as LoginFormDefault } from './LoginForm'
export { default as GoogleAuthButtonDefault } from './GoogleAuthButton'